<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/style.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@400;700;900&display=swap"
      rel="stylesheet"
    />

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
    />
    <title>Anime Gallery</title>
  </head>
  <body>
    <nav>
      <div class="logo">
        <img src="../assets/anime.webp" alt="error" class="navImg" />
        <span class="galleryTitle">ANIME GALLERY</span>
      </div>
      <div class="links">
        <a href="http://localhost:8080/posts/new"><i class="fa-solid fa-circle-plus"></i> Create</a>
        <a href="http://localhost:8080/posts"><i class="fa-solid fa-house"></i> Home</a>
        <a href="#"><i class="fa-solid fa-circle-info"></i> Info</a>
      </div>
    </nav>

    <% for(post of posts) { %>
    <div class="gallery">
      <h3 class="username">@<%=post.username%></h3>
      <h4 class="caption">❝<%=post.thaught%>❞</h4>
      <% for( let index = 0; index < 2;index ++) { %> <% if (post.image[index]=="") { %> <% index --
      %> <% } %>
      <img src="<%=post.image[index]%>" alt="" />
      <% } %>
      <div class="info">
        <a href="http://localhost:8080/posts/<%=post.id%>"
          >Click here to see All <%=post.image.length%> posts</a
        >
      </div>
      <div class="update">
        <a href="http://localhost:8080/posts/update/<%=post.id%>">Update Post</a>
      </div>
      <div class="delete">
        <form method="post" action="/posts/<%=post.id%>?_method=delete">
          <button>Delete Post</button>
        </form>
      </div>
    </div>

    <% } %>
  </body>
</html>
